[package]
name = "seiton-device-api"
description = "Device abstraction: DeviceWatcher, DeviceSource and capabilities."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[features]
# In-memory DeviceSource for tests in this and downstream crates.
fake = []

[dependencies]
bitflags = { workspace = true }
bytes = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
